let myNumber = 0;

function handleClick() {
  myNumber = myNumber + 1;
  console.log(myNumber);
  // const element = document.querySelector('#my-text');
  // const newSize = Math.random() * 60 + 10;
  // element.style.fontSize = `${newSize}px`;
}